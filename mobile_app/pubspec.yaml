name: brandpilot
description: AI Advertising & Marketing App
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_riverpod: ^2.4.0
  go_router: ^13.0.0
  google_fonts: ^6.0.0
  intl: ^0.19.0
  http: ^1.1.0
  image_picker: ^1.0.0
  flutter_dotenv: ^5.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
